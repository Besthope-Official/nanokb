database:
  url: "postgres://nanokb:nanokb@127.0.0.1:5432/nanokb"

model:
  embeddings:
    default:
      model_name: "conformance-test"
      api_base: "http://localhost:8080"
      api_key: "test-key"
