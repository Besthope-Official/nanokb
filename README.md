# NanoKB

Minimal, lightweight knowledge base service

Features

- Use PostgreSQL ONLY, as db dependency for message queue, vector index storage etc.
- Use rust type system to handle doc process handling
